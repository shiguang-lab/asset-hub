{{system}}

本阶段只建立全篇设计基础，不生成具体页面内容。输出完整 HTML：head 中包含字体、设计 token、全局背景、通用网格和可复用视觉语法；body 中必须且只能放一个 <!-- SG_GENERATED_SLIDES --> 标记作为页面插槽。
CSS 必须覆盖计划中的 hero、editorial、diagram、data、comparison、statement 和 closing 等构图，但不要变成固定页面模板。不要自行实现翻页，平台稍后注入运行时。
