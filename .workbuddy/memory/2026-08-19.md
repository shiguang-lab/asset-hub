# 2026-08-19

## 文档列表垂直居中修复

- 修复 `apps/web/src/styles/documents.ts` 中文档列表表格视图行内容垂直居中问题
- 名称列：`.sg-asset-name-copy` 加 `gap: 0` 去掉 row-gap 影响，配合 `justify-content: center` 和 `min-height: 32px` 居中
- 可见范围列：`.sg-vis` 加 `line-height: 1`，`.sg-vis svg` 加 `margin-top: -2px` 修正 lucide 图标偏移
- 已 build 并部署到 CloudStudio：https://05485dbaf5a848868cab4728daec2c7e.app.workbuddy.link
- 用户反馈线上 `.sg-asset-name-copy` 仍有 gap，重新 clean build 并重新部署

## 部署 web 到 NAS

- NAS 部署目录 `/home/yanxianliang/asset-hub`，SSH 别名 `ugreen-nas`（100.87.115.78，用户 yanxianliang，密钥 ~/.ssh/ugreen_ed25519）
- NAS 上无 git，部署方式是手动同步代码 + `docker compose build`
- 部署脚本：`infra/compose/deploy.sh`，runtime.env 里 IMAGE_TAG 固定为 `20260818-045451-amd64`（prepare-runtime-env.sh 已存在则保留）
- 同步方式：本地 `tar czf - <files> | ssh ugreen-nas 'cd /home/yanxianliang/asset-hub && tar xzf -'`（NAS 无 scp，用 ssh 管道）
- 只 build + 重启 web：`docker compose --env-file runtime.env -f production.yml build web` + `up -d web`
- web 端口映射绑定 NAS_BIND_IP=100.87.115.78，宿主机 curl 需用 100.87.115.78:3700（127.0.0.1 不通）
- 验证：docker exec 进容器 grep 新 bundle 已含 `gap: 0` 修复
