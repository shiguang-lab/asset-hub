# 2026-08-23

## 在线演示编辑器 UI 重构（对标 WPS/Office 网页版）

### 第二轮：右侧属性面板重构（跟随选区 + 分组折叠）
用户指出右侧配置 UI 组织太差、且不该显示整页所有元素，应"选了文案A只显示A的配置"，参考 Google Slides/Canva/Figma 的 inspector 范式。
- 当前演示框架：项目自研轻量 HTML 演示格式（`<section data-sg-page>` + `data-sg-id/kind/enter/hover`），非 reveal.js 等现成框架。
- 改动 `presentations.tsx`：
  - 引入 antd `Collapse`；新增 `selectedEl = elements.find(e=>e.id===selectedId)` 跟随选区。
  - 新增 `ElementInspector` 组件：按 `el.kind` 只渲染该类型支持的配置，用 Collapse 分「内容 / 动画」两组折叠；未选中时显示本页概览（页标题+元素数+引导）。
  - 主题作为全局项保留在面板顶部；底部提示精简。
  - 字段名 `<label>` 改为 `<span className="sg-el-label">`（避开 biome a11y noLabelWithoutControl 误报）。
- 改动 `presentations.ts`：新增 `.sg-el-*` 全套样式（右侧面板 flex 列、分区、概览卡、inspector 头、字段组、折叠样式）。
- 验证：tsc + biome + vite build 全绿。EditableElement 底层暂无颜色/字号/对齐字段，本轮只接已有能力（内容+动画+Hover），样式类配置预留未伪造。

用户指出在线演示编辑页（PresentationEditorPage）UI 不符合业内主流，要求两项优化：
1. 左侧 slices 操作栏下沉：删除顶部 5 按钮操作栏，改为切片右键菜单（复制/上移/下移/删除）+ 切片间分割线 hover 出现"＋插入页" + 列表底部固定"新建页面"按钮。
2. 中间内容区改为 WPS 风格工作区：中性工作区底色，16:9 画布居中并随视口等比缩放(fit)，四周留工作区边距带阴影，取消原 iframe 写死的 minHeight:560（原写法导致宽屏左右大留白、上下突兀空白）。

### 改动文件
- `apps/web/src/features/presentations/presentations.tsx`
  - 引入 antd `Dropdown`（右键菜单），删除不再使用的 `Tooltip`/`Popconfirm`/`astAddPage`。
  - 新增底层封装：`insertPageAfter(i)`（分割线插入）、`removePageById`/`movePageById`/`duplicateActivePageById`（右键菜单按 id 操作），删除了已无调用的 `removeActivePage`/`moveActivePage`/`duplicateActivePage`。
  - 重构左侧栏 JSX 与中间工作区 JSX（`.sg-slide-workarea` > `.sg-slide-stage` > iframe）。
- `apps/web/src/styles/presentations.ts`：在 PresentationsGlobalStyles 补全全部 `.sg-slide-*` 布局样式（三栏 grid、缩略图卡、分割线插入按钮、底部新增、WPS 工作区、16:9 等比舞台）。原 `.sg-col`/`.sg-slide-editor` 等类此前在样式库完全无定义，靠内联 style 撑着。
- `packages/content/src/presentation-ast.ts`：新增 `insertPage(tree, page, afterId?)`，供编辑器"分割线插入/底部新增"复用；已 `tsc -p` 重建 dist 并 export 出去。

### 验证
- `tsc -b` 类型检查通过；biome check 干净；`vite build` 成功（14 tasks successful）。
- 注意：本地无 `.env.local`（broker 配置），未真正起 dev server 在浏览器点验交互；编译链路全绿，浏览器交互（右键菜单/分割线插入/等比缩放观感）需用户本地 `cd apps/web && node node_modules/vite/dist/node/cli.js --port 3000` 自测。

## 第三轮：补齐多类型元素配置 + 双击画布内联编辑文本

用户指出：不止文字能编辑（协议支持 text/image/link/chart/counter/code-island 六种）、右侧 panel 之前只对 text/image/link 做了控件，chart/counter/code 没接；并希望文字能像主流产品（Google Slides/WPS/Canva）那样双击画布直接编辑，而非右侧表单。

### 改动
- `packages/content/src/presentation-ast.ts`：`EditableElement` 接口新增 `chart?`/`counter?` 字段；`listEditableElements` 提取 `data-sg-chart`/`data-sg-counter`（hast camelCase `dataSgChart`/`dataSgCounter`）。已重建 dist。
- `apps/web/src/features/presentations/presentations.tsx`：
  - `ElementInspector` 补齐三类配置：chart（类型 select + 标签/数据逗号分隔输入 + 原始 JSON 折叠编辑）、counter（目标数值 input）、code-island（源码 textarea，复用 onText）。
  - text 类：右侧"文本内容"改为提示"双击画布直接编辑" + 折叠的备用 textarea，避免与画布内联编辑冲突。
  - 预览桥接脚本 `SG_EDIT_BRIDGE` 新增内联编辑：给 `[data-sg-kind="text"]` 加 `dblclick→contenteditable`，失焦/回车提交、ESC 取消并还原，提交时 `postMessage({source:"sg-editor-preview",type:"edit-text",id,text})`；CSS 加 `.sg-editor-editing` 编辑态样式与 `[data-sg-kind=text]{cursor:text}`。前端 `onMessage` 新增 `edit-text` 分支调 `updateTextContent`。
  - 新增帮助函数 `safeParseChart/chartToJson/splitLabels/splitNumbers`。
- `apps/web/src/styles/presentations.ts`：新增 `.sg-el-hint-inline`、`.sg-el-raw`（details 折叠）样式。

### 验证
- content 包 `npm run build` 成功；web `tsc -b` 通过；biome 仅 1 个 unsafe-fix 警告（空 fragment，无关）；`vite build` 14 tasks successful。

### 边界（下期可做）
- 文本颜色/字号/对齐等样式仍无底层字段，需先在 content 包扩 `data-sg-color/align/size` 协议并让 presentation-runtime 消费，再在右侧/画布接控件。本轮未做（用户选择"本期全做不含样式"）。

## 第四轮：文本样式协议扩展（颜色/对齐/字号真正可编辑）

用户确认"做下一期"——即把第三轮预留的文本样式能力真正落地。

### 关键约束（先调研确认）
- 渲染层 `presentation-runtime.ts` 的 `SG_RUNTIME_CSS` 此前完全不消费 `data-sg-color/align/size`，样式全靠 CSS 类 + CSS 变量。因此扩协议必须同步让 runtime 消费，否则属性无效。
- 编辑器预览 `buildPreviewSrcDoc` 直接把 `renderPresentationHtml` 产出当 srcDoc，runtime 的 `<style>` 已注入，所以 runtime 加规则预览自动生效（缩略图也复用同一 style）。

### 改动
- `packages/content/src/presentation-ast.ts`：`EditableElement` 加 `color?`/`align?`/`size?`；`listEditableElements` 提取 `data-sg-color`/`data-sg-align`/`data-sg-size`（hast key `dataSgColor`/`dataSgAlign`/`dataSgSize`）。已重建 dist。
- `packages/content/src/presentation-runtime.ts`：
  - `SG_RUNTIME_CSS` 加 `[data-sg-align="left|center|right"]{text-align:...}` 与 `[data-sg-size="sm|md|lg|xl"]{font-size:0.8/1/1.35/1.8em}`（size 用 em 相对当前 clamp 字号，安全不破坏布局）。
  - `SG_RUNTIME_JS` 顶层新增 `applyElementStyles()`：遍历 `[data-sg-color]` 把属性值应用为 `el.style.color`（因 CSS `attr()` 在 color 上兼容性不可靠，故用 JS 应用；align/size 走纯 CSS）。
- `apps/web/src/features/presentations/presentations.tsx`：
  - `ElementInspector` props 加 `color?`/`align?`/`size?`；新增 `styleItem`（仅 `kind==="text"` 显示）分组折叠，含：颜色色板（8 个语义色 `TEXT_COLOR_SWATCHES` + 原生 color input 自定义）、对齐 `ALIGN_OPTIONS`、字号 `SIZE_OPTIONS`；均通过 `onAttr(...,"data-sg-*",v)` 写入，空值写 ""。Collapse items 改为 `[contentItem, styleItem, motionItem]`。
  - 新增常量 `TEXT_COLOR_SWATCHES`/`ALIGN_OPTIONS`/`SIZE_OPTIONS`。
- `apps/web/src/styles/presentations.ts`：新增 `.sg-el-color-row`/`.sg-el-swatch`(active 高亮)/`.sg-el-color-input` 样式。

### 验证
- content 包 `npm run build` 成功（exit 0）；web `tsc -b` 通过；biome 仅 2 个 unsafe-fix 警告（空 fragment，无关）；`vite build` 14 tasks successful。
- 注意：本地无 `.env.local`，未起 dev server 点验；请用户本地 `cd apps/web && node node_modules/vite/dist/node/cli.js --port 3000` 自测——选中文本元素后右侧出现"样式"分组，改颜色/对齐/字号应即时在画布预览与缩略图生效。

## 第五轮：补齐样式/快捷键/空状态（继续扩展）

用户问"还有哪里需要优化或扩展，可以继续执行后续"。基于前四轮代码现状（图片/链接样式缺失、文本仅颜色/对齐/字号、无键盘效率、无空状态）直接执行补齐，无需打断确认。

### 改动
- `packages/content/src/presentation-ast.ts`：
  - `EditableElement` 加 `weight?`(bold/normal)/`fontStyle?`(italic/normal)/`radius?`(none/sm/md/lg/full)/`border?`(on)/`shadow?`(on)；
  - `listEditableElements` 提取 `dataSgWeight`/`dataSgStyle`/`dataSgRadius`/`dataSgBorder`/`dataSgShadow`；
  - 新增 `removeElement(tree, id)`（从 DOM 树删节点）+ 内部 `findParent` 辅助。已重建 dist。
- `packages/content/src/presentation-runtime.ts`：
  - `SG_RUNTIME_CSS` 加图片外观覆盖：`[data-sg-radius=*] img`(none/sm/md/lg/full 圆角)、`[data-sg-border=on] img`(描边)、`[data-sg-shadow=on] img`(投影)；
  - `SG_RUNTIME_JS` `applyElementStyles()` 扩展：除 color 外，把 `data-sg-weight`→`fontWeight`(700/空)、`data-sg-style`→`fontStyle`(italic/空) 应用为内联样式（与 color 同理 attr() 不可靠）；并暴露 `SG.applyElementStyles` 供外部重跑。
- `apps/web/src/features/presentations/presentations.tsx`：
  - `ElementInspector` props 加 `weight/fontStyle/radius/border/shadow`；
  - 链接类加颜色色板（复用 `TEXT_COLOR_SWATCHES`）；
  - 文本 `styleItem` 扩展：加字重 `WEIGHT_OPTIONS`(常规/加粗)、字形 `FONT_STYLE_OPTIONS`(正体/斜体)；
  - 新增图片 `imageStyleItem`（仅 kind==="image"）：圆角 `RADIUS_OPTIONS` + 边框/投影复选开关；
  - Collapse items 改为 `[contentItem, styleItem, imageStyleItem, motionItem].filter(nonnull)`；
  - 新增快捷键 effect：Delete/Backspace 删选中元素（非内联编辑态）、Cmd/Ctrl+D 复制当前页；新增 `deleteSelectedElement`（调 `astRemoveElement`）；
  - 导入 `removeElement as astRemoveElement`；
  - 中间工作区空状态：pages 为空时显示 `.sg-slide-empty` 引导卡片 + 新建按钮。
- `apps/web/src/styles/presentations.ts`：新增 `.sg-slide-empty`/`.sg-slide-empty-card`、`.sg-el-switch-row`/`.sg-el-switch` 样式。

### 验证
- `packages/content` `npm run build` 成功；
- web `npx tsc --noEmit` 通过；
- biome：formatter 已 `--write` 修复，剩 3 个 `useOptionalChain` warnings（均为 `el.properties ?? {}` 已安全包裹处的 false-positive，未强制改以防引入隐患）；
- `vite build` 成功（index 5MB，gzip 1.59MB，chunk size 警告与本次无关属历史）。
- 仍未起 dev server 点验（无 .env.local）；交互需用户本地自测。

### 仍可选后续（未做，供参考）
- 元素层级 z-index / 自由拖拽定位（当前元素靠 HTML 流布局，无绝对定位）；
- 多选 / 框选；
- 撤销栈颗粒度细化（当前整体 html 快照，元素级撤销已够）；
- 图片裁剪 / 滤镜；
- 图表类型扩展（雷达/漏斗等，当前 bar/line/pie）；
- 主题色板自定义（当前 5 套预设）。

## 第六轮：元素自由拖拽定位 + 多选框选（继续"继续把"）

用户说"继续把"，从第五轮末尾列的候选里挑了最核心的"元素自由拖拽定位 / 多选框选"执行。对标 Google Slides/WPS/Canva 的可编排能力。

### 设计
- 坐标协议 `data-sg-x`/`data-sg-y`（百分比字符串，相对页面宽高），页面 `[data-sg-page]` 改 `position:relative` 容器，元素 `[data-sg-x]` 走 `position:absolute;left:var(--sg-x);top:var(--sg-y)`。
- 因 CSS `attr()` 在自定义属性不可靠，x/y 同 color 用 JS 应用：`SG_RUNTIME_JS.applyElementStyles()` 遍历 `[data-sg-x]` 设 `--sg-x/--sg-y` 变量；并暴露 `SG.applyElementStyles` 供外部重跑。
- 拖拽/框选全在 iframe 内 `SG_EDIT_BRIDGE` 完成，通过 `postMessage` 回写：拖拽松手发 `move`(items:[{id,x,y}])，框选松手发 `select-multiple`(ids:[])；编辑器收到后 `applyAst` 批量写属性 / 设 `selectedIds`。
- 编辑器状态从单一 `selectedId` 升级为 `selectedIds:string[]`（派生 `selectedId = selectedIds[0] ?? null` 兼容旧引用）。高亮 postMessage 改传 `ids`；bridge `highlight` 分支按 ids 数量加 `.sg-editor-selected`(单)或 `.sg-editor-multi`(多)，并存 `window.__sgSelected` 供拖拽整体平移判定。

### 改动
- `packages/content/src/presentation-ast.ts`：`EditableElement` 加 `x?`/`y?`；`listEditableElements` 提取 `dataSgX`/`dataSgY`；已重建 dist。
- `packages/content/src/presentation-runtime.ts`：`SG_RUNTIME_CSS` 页面加 `position:relative` + `[data-sg-x]{position:absolute;left:var(--sg-x,0%);top:var(--sg-y,0%)}`；`applyElementStyles` 扩展 x/y → CSS 变量。
- `apps/web/src/features/presentations/presentations.tsx`：
  - `SG_EDIT_BRIDGE` 重写：新增 mousedown 拖拽（选中元素或当前多选整体平移，百分比换算基于页面 getBoundingClientRect）+ 空白框选（画 `.sg-marquee` 收集中心命中元素）；原 click/dblclick/msg 逻辑保留并适配多选；新增 `.sg-editor-multi`/`.sg-editor-dragging`/`.sg-marquee` 样式。
  - `selectedIds` 状态 + 派生 `selectedId`；`onMessage` 加 `select-multiple`/`move` 分支；高亮发 `ids`。
  - 新增 `deleteSelectedElements`（批量删）、`alignSelected`（left/center-x/top/middle-y 批量对齐）。
  - 快捷键 effect 删元素改 `deleteSelectedElements`（批量）。
  - 右侧面板：无选→概览；多选(>1)→`sg-el-multi-bar` 批量操作条（左/水平居中/顶/垂直居中/批量删除）；单元素→原 `ElementInspector`。
- `apps/web/src/styles/presentations.ts`：新增 `.sg-el-multi-bar`/`.sg-el-multi-actions` 样式。

### 验证
- content `npm run build` ok；web `tsc --noEmit` 通过；biome `--write` 修格式，剩 ~7 个 `useOptionalChain`/非空断言 warnings（历史代码 false-positive 与本次 `!` 断言，均非 error）；`vite build` 成功。
- 仍未起 dev server 点验（无 .env.local）。自测：拖拽画布元素应实时移动、松手落位；空白处拖出框选应多选；右侧出现批量条；Delete 批量删。

### 仍可选后续（更新）
- 元素层级 z-index（data-sg-z，runtime 加 z-index 变量）；
- 缩放手柄（四角拖拽改尺寸，需 data-sg-w/h 协议）；
- 对齐参考线 / 智能吸附；
- 图片裁剪 / 滤镜；图表类型扩展；主题色板自定义。

## 第七轮：缩放手柄 + 尺寸协议 + 智能吸附参考线（续第六轮拖拽/多选）

### 落地
- 尺寸协议 `data-sg-w`/`data-sg-h`（百分比相对页面宽/高），runtime `[data-sg-x]{...width:var(--sg-w,auto);height:var(--sg-h,auto)}`，`applyElementStyles` 扩展 w/h → `--sg-w/--sg-h`；AST `EditableElement` 加 `w?`/`h?` 并提取 `dataSgW/dataSgH`；已重建 dist。
- 桥接脚本 `SG_EDIT_BRIDGE` 重写增强：
  - 单元素选中后渲染 8 个 resize handle（`.sg-handle` nw/n/ne/e/se/s/sw/w），mousedown 命中 handle 进入 resize，按页面比例换算宽高（角手柄同时改 x/y，锚点左上）。
  - 拖拽/缩放时计算吸附：参考线=页面中线 + 其他可定位元素的左/中/右、上/中/下边（百分比）；候选值差 < 6px（SNAP）吸附并画 `.sg-guide`（粉线，h/v 两类）。
  - 工具函数：`getBBox`(取/估算百分比 bbox 并写回)、`collectGuides`、`snapAxis`、`drawGuides`、`renderHandles`、`clearHandles/clearGuides`。
  - `highlight`/`navigate` 消息分支：单元素选渲染 handles，清除 handles+guides；多选/翻页清除。
- 编辑器 `onMessage` 加 `resize` 分支（item:{id,x,y,w,h}）→ `applyAst` 写 4 属性进同一撤销快照。
- 右侧 `ElementInspector` 新增「尺寸与位置」分组（X/Y/宽/高 数字输入，百分比），所有定位元素通用；内联 prop 类型同步加 x/y/w/h（修复 tsc 报错：原手写内联类型缺这 4 字段）。
- 样式：`.sg-el-pos-row`/`.sg-el-pos-col`/`.sg-el-hint-sm`；handle/guide 样式内联在 bridge CSS。

### 验证
- content build ok；web tsc 通过（清 tsconfig.tsbuildinfo 后仍报，根因是 `ElementInspector` 的 `el` 内联类型副本未含 x/y/w/h，补字段后通过——注意：此类手写内联类型与 AST 易失同步，后续扩 `EditableElement` 必须同步该处）；biome 剩 7 warnings（历史 false-positive）；vite build 成功。
- 未起 dev server 点验。自测：拖元素角手柄改尺寸、拖拽/缩放靠近中线或其他元素边出现粉色吸附参考线并自动对齐、右侧尺寸组精确输入。

### 仍可选后续
- 元素层级 z-index（data-sg-z）；图片裁剪/滤镜；图表类型扩展（雷达/漏斗）；主题色板自定义；旋转手柄（data-sg-rot）。

## 第八轮：元素层级 z-index + 旋转手柄 data-sg-rot（续第七轮变换能力）

### 落地
- 协议 `data-sg-z`（整数层级）/ `data-sg-rot`（度数，如 "15"→15deg）；runtime `[data-sg-x]{...z-index:var(--sg-z,auto);transform:rotate(var(--sg-rot,0deg));transform-origin:center}`；`applyElementStyles` 扩 z/rot → `--sg-z/--sg-rot`；AST `EditableElement` 加 `z?`/`rot?` 并提取 `dataSgZ/dataSgRot`；已重建 dist。
- 桥接脚本 `SG_EDIT_BRIDGE`：单元素选中除 8 resize handle 外，新增顶部旋转手柄 `.sg-rotate`（带连接线伪元素）；mousedown 识别 `data-sg-rotate` 进入旋转，按元素中心角 `atan2` 实时算角度（吸附 1° 取整）写 `--sg-rot`，松手 `postMessage rotate`(rotItem:{id,rot})；旋转手柄位置按未旋转 bbox 定位（旋转中手柄不动，符合主流）。
- 编辑器 `onMessage` 加 `rotate` 分支（写 data-sg-rot 进撤销栈）；`ElementInspector` 内联类型同步加 z/rot。
- 右侧「尺寸与位置」分组加：层级数字输入 + 旋转角度输入 + 置顶(999)/置底(-999) 按钮（所有定位元素通用）。
- 样式：`.sg-rotate`（圆点+连线）、`.sg-el-z-row`。

### 验证
- content build ok；web tsc 通过（内联 prop 类型已同步 z/rot）；biome 剩 7 warnings（历史 false-positive）；vite build 成功。
- 未起 dev server 点验。自测：拖顶部圆点旋转元素、右侧旋转输入角度、层级输入/置顶置底、旋转后缩放手柄仍基于未旋转 bbox。

### 几何变换能力已完备
坐标(x/y) + 尺寸(w/h) + 层级(z) + 旋转(rot) + 吸附参考线 + 多选框选 + 批量对齐。
### 仍可选后续（非几何）
- 图片裁剪 / 滤镜；图表类型扩展（雷达/漏斗）；主题色板自定义；元素锁定/隐藏；撤销栈元素级细分。

## 第九轮：元素锁定/隐藏 + 图表类型扩展（雷达/漏斗）

### 落地
- 协议新增 `data-sg-lock`（on=锁定）、`data-sg-hidden`（on=隐藏）；`EditableElement` 加 `lock?`/`hidden?` 并提取 `dataSgLock/dataSgHidden`；已重建 content dist。
- runtime CSS：`.sg-hidden{display:none!important}`（编辑预览与播放一致）；`SG.chart` 的 `toEChartsOption` + 原生 SVG 兜底均新增 `radar`/`funnel`（雷达无 ECharts 时画网格多边形，漏斗画梯形层）。
- 编辑器 `ElementInspector`：内联 `el` 类型同步加 `lock?`/`hidden?`；新增「状态」分组（锁定/隐藏开关），位于尺寸之后、样式之前；图表类型 Select 加 雷达图/漏斗图 两项。
- 桥接脚本 `SG_EDIT_BRIDGE`：锁定元素（data-sg-lock=on）不响应拖拽/缩放/旋转（mousedown 早退），不渲染手柄，`::after/::before` 画紫色锁标；隐藏元素 display:none 自然不可点，框选已跳过零尺寸元素防误命中。
- 编辑器 `deleteSelectedElements`：跳过锁定元素（toast 提示先解锁），键盘 Delete 同样保护。
- 样式复用 `.sg-el-hint-sm`/`.sg-el-switch-row`（stateItem 无新增 class）。

### 验证
- content build ok；web tsc 通过（内联 prop 类型已同步）；biome 剩 7 历史 warnings（含 pre-existing selectedEl 未用变量 false-positive）；vite build 成功。
- 未起 dev server 点验。自测：右侧状态组勾选锁定后画布元素出现锁标且不可拖拽、键盘删无效；勾选隐藏后元素消失；图表改雷达/漏斗类型渲染对应图形。

### 仍可选后续
- 图片裁剪/滤镜；主题色板自定义；撤销栈元素级细分；锁定元素在左侧缩略图/元素列表标注。

## 第十轮：图片裁切 + 元素复制 + 主题色板 + 播放增强（用户："能做的全部帮我做完"）

### 落地（4 项，全部零外部依赖、编译可验证）
1. **图片裁切（fit）**：`EditableElement.fit?` + `dataSgFit` 提取；runtime 加 `[data-sg-fit] img{object-fit:var(--sg-fit,cover)}` 与 cover/contain/fill 三个值；`applyElementStyles` 把 fit 写 `--sg-fit`；Inspector 外观分组加「填充方式」Select（FIT_OPTIONS）。`[data-sg-kind=image] img{width/height:100%}` 保证自由定位尺寸生效。
2. **元素复制**：`ast.duplicateElements(tree, ids, dx=4, dy=4)` 复制（JSON clone + 偏移 + 锁定跳过），返回新 id；编辑器 `duplicateSelectedElements`；快捷键 `Cmd/Ctrl+Shift+D`（原 `Cmd+D` 复制页保留）；多选栏加「复制」按钮。锁定的元素不复制（toast 提示）。
3. **主题色板自定义**：`themeCssVariables(theme, custom?)` 支持自定义 5 变量；新增 `THEME_PRESETS`（7 套：紫/蓝/绿/橙/红/暗夜/极简灰）+ `ThemePalette`；编辑器 `applyThemePreset`/`applyCustomPrimary`；右侧主题区加可视化配色色板（点击预设 + 主色 color input），`isSamePalette` 高亮当前。
4. **播放页增强**：`PresentationPlayerPage` 加 顶部✕退出按钮（Esc 退出到编辑页）、底部页码圆点跳转（监听 iframe postMessage `sg:page`，点选发 `sg:goto`）；runtime `SG.presentation` 在 show 时 `postMessage` 上报页码，并监听 `sg:goto` 跳转。`useEffect` 加 Esc + message 监听。

### 验证
- packages/content `npm run build` ok（ast + runtime + presentation 三处改动均进 dist）。
- web tsc 通过（`e.shift`→`e.shiftKey` 修正；ThemePalette 导入）；biome 自动修复 1 处（主题色板 color input 可选链），剩 9 历史 warnings；vite build 成功。
- 未起 dev server 点验。自测建议：图片选 contain 看完整图、cover 裁切；选区 Cmd+Shift+D 复制并偏移；主题点蓝色板主色变蓝；播放页 Esc 退出、点底部圆点跳页。

### 说明
- fit 的 cover 为默认（保兼容历史图片无 data-sg-fit 时仍填满）。
- 元素复制偏移 4%/4%，避免与原文重叠但仍可见。
- 播放页页码跳转依赖 iframe postMessage；runtime 已上报，srcDoc 沙箱同源（无 allow-same-origin 但 postMessage 不受限）。

## 第十一轮：重方向三件套（用户："重方向也帮我实现吧"）

### 落地（3 项）
1. **图片滤镜**：`EditableElement.filter?` + `dataSgFilter` 提取；runtime 加 `[data-sg-kind=image] img{filter:var(--sg-filter,none)}` 与 8 预设（none/grayscale/sepia/warm/cool/blur/brightness/contrast），JS 写 `--sg-filter`；Inspector 外观分组加「滤镜」Select（FILTER_OPTIONS）。content 已 build。
2. **撤销栈元素级细分（智能合并）**：`useHistory.set(updater, mergeKey?)` 加 mergeKey + 600ms 时间窗合并；`applyAst(fn, mergeKey?)` 透传；属性编辑 `editAttr` 用 `attr-<id>-<name>`、桥接 move/resize/rotate 用 `drag-<id(s)>`。效果：连续调同一属性/一次完整拖拽合并为单步撤销，避免历史帧爆炸。整页/复制/删除不传 mergeKey（独立步）。
3. **AI 生成元素**：后端 `runAiEdit` 加 `scope:"insert"`（在 targetId 页末尾新增元素、不改其它，scopeHint 明确约束）；`ai-edit` 端点 z.enum 扩展。web 加 `aiInsert` mutation + `AI_INSERT_KINDS`（7 类：段落/小标题/卡片/图表/列表/引用/指标）、`✦ AI 生成元素` 按钮 + 独立 Modal（类型 Select + 描述），生成后 `setHtml` 应用。后端 dev 走 `tsx watch src`（无需 build）。

### 验证
- packages/content `npm run build` ok；web tsc 通过；biome 剩 9 历史 warnings（含主题色板 color input 可选链 safe 版）；vite build 成功；apps/api `tsc --noEmit` 通过。
- 未起 dev server 点验。自测：图片选「黑白/复古」看滤镜；连续拖字号滑块后撤销只退一步；图片选滤镜；顶部「✦ AI 生成元素」选「图表」+ 描述 → 当前页末尾插入新图表元素。

### 编辑器能力全景（截至本论）
布局/选区配置/内联编辑/文本样式/图片样式(圆角边框阴影+裁切+滤镜)/链接/富文本/快捷键/空状态/拖拽多选/缩放吸附/层级旋转/锁定隐藏/图表(5类)/元素复制/主题色板/播放控制(Esc+跳页)/撤销智能合并/AI生成元素。
