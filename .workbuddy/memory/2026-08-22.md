# 2026-08-22

## 发布 apps/web 到 NAS（单应用部署）
- 按 nas-tar-deploy skill 的「只发单个应用」流程，将 HEAD 47bf03a（web 样式抽取重构）发布到 NAS。
- 备份：`deploy-backups/source-before-web-deploy-20260822-023707.tar.gz`（91M）。
- tar 管道全量同步工作树（排除 .git/node_modules/dist/.next/.env/runtime.env/.workbuddy 等），保留 NAS 上的 `.env` 与 `runtime.env`。
- `runtime.env` 的 IMAGE_TAG 升级为 `20260822-web-styles-amd64`。
- 仅 `docker compose build web` + `up -d web`（未跑全量 deploy.sh，其余 5 容器保持旧 tag `20260821-comments-ui-amd64` 不变）。
- 验证：`http://100.87.115.78:3700/healthz` 200，页面 200（title 知序 · AI 知识与创作），web 容器 healthy。
- 注意：本次只发 web，packages/config、packages/database、packages/contracts 及 apps/ssr 的改动（含 05c36b6 提交）未随本次部署上线，后续如涉及 api/ssr 需全量发布。

## 统一 Tabs 组件（替换各页面自研 tab）
- 根因：assets/tasks/presentations/knowledge/shell 搜索预览 6 处页面各写了一套 `role="tab"` 自研 tab 按钮，且字体极小（tasks 12px、presentations 10px、knowledge 10.5px），只有资产页 13px 正常——用户期望全部统一为 antd Tabs。
- 新建可复用组件 `apps/web/src/shared/AppTabs.tsx`：基于 antd Tabs，样式精确复刻资产页 `sg-type-chip` 观感（13px/muted/#c6b1ff 选中/缩进 2px 高亮线/数字徽标 em），只渲染 tab 头（隐藏 antd content-holder），内容由页面按 activeKey 渲染。
- `theme/tokens.ts` Tabs 组件 token 全局定制：titleFontSize 13、itemColor #777489、itemHoverColor #b8b5c9、itemSelectedColor #c6b1ff、inkBarColor #7c3cff、horizontalItemGutter 2、horizontalItemPadding 9px 12px 12px、horizontalMargin 0 0 14px。
- 替换 6 处：assets(带 count)、tasks 列表(带 count)+详情(sources count)、presentations、knowledge scope+detail、shell 搜索预览。
- 清理旧样式：styles/assets.ts（sg-type-bar/sg-type-chip）、tasks.ts（sg-task-tabs/sg-task-detail-tabs）、presentations.ts、knowledge.ts（scope/detail）、search.ts（preview-tabs）、shell.ts（sg-tabs/sg-tab），共 -360 行。
- 删除 6 个 0 引用死代码文件：features/*/*.styles.ts（assets/assistant/document-cards/document-tree/documents/home，上次提交遗留的 createStyles 半成品）。
- 验证：`tsc -b`、`vite build` 均通过；biome lint 已修复 import 排序。
- 注意：editor.tsx / asset-detail / settings / datasets / knowledge 顶部本就用的 antd Tabs，随全局 token 统一为 13px。改动未发布到 NAS，需重新单发 web。

## Tabs 样式 Token 化改造 + 样式约束文档
- 按用户要求：公共 Tabs 样式改为「Theme Token 为主 + antd-style 兜底」，不用全局 CSS/less。
- 关键发现：antd-style createStyles 回调的 token 是「全局 AliasToken」，**不含组件专属 Token**（如 Tabs 的 itemColor/titleFontSize），所以组件专属外观必须配置在 `tokens.ts` 的 `components.Tabs`，createStyles 里重复写会覆盖 Token 导致主题失效。
- AppTabs.tsx 重构：createStyles 移除 itemColor/titleFontSize/horizontalItemPadding/inkBarColor 等冲突属性，只留 Token 表达不了的 3 件事：①隐藏 600 字重占位撑宽（选中加粗不抖动）②数字徽标 em ③ink-bar 圆角/高度。tokens.ts 的 Tabs 补 `colorBorderSecondary: #2a2731`（nav 底部边框对齐资产页 --sg-border）。
- 约束文档：docs/apps/web.md 新增 3.3.1「组件外观：Theme Token 优先，antd-style 只补 Token 表达不了的」，并强化 3.3 禁止新增业务 .css/.less，全局样式用 antd-style、组件用 createStyles/createGlobalStyle、由 ThemeProvider 挂载。
- 验证：tsc -b、vite build、biome lint 全通过；dev server HMR 正常；通过 dev 抓模块确认 Token 链路生效（AppTabs 无冲突属性、tokens.ts 含目标值）。
- 后续约定：web 新样式一律优先 antd-style + Theme Token，不写全局 CSS/less；历史 styles/*.ts 全局样式属遗留，新代码不得新增。

## 本地调试 web（接口/授权走线上）
- `.env.local` 已配 broker：ASSET_HUB_LOCAL_BROKER_ENABLED=true + 账号 yanxianliang + ASSET_HUB_AUTH_TARGET=https://shiguanglab.com。
- 改过 `.env.local`：ASSET_HUB_LOCAL_API_TARGET 从 localhost:3001 改为 `http://100.87.115.78:3701`（线上 API），使接口走线上。
- 启动：`cd apps/web && node node_modules/vite/dist/node/cli.js --port 3000 --strictPort`（注意不能用 `node .bin/vite`，那是 shell 脚本会报语法错）。
- 验证链路：/api/auth/session 返回 authenticated:true 的线上身份（broker 注入 x-sg-identity）；/api/v1/assets 返回线上真实数据；dev 强制监听 127.0.0.1。

## 统一 Table 组件（替换所有原生 table 为 antd Table + antd-style）
- 新建 `apps/web/src/shared/AppTable.tsx`：基于 antd Table 封装，泛型支持，统一 `size="middle"`、默认 `pagination={false}`、`scroll={{ x: "max-content" }}`。
- `theme/tokens.ts` 新增 Table 组件 token：headerBg #17151b、headerColor #777489、rowHoverBg #17151c、rowSelectedBg rgba(124,60,255,0.14)、cellFontSize 13、borderColor #232129、cellPaddingBlock 12、headerBorderRadius 0、selectionColumnWidth 40。
- 替换 11 个页面共 13 个表格：assets（多选 rowSelection+行点击 onRow+下拉菜单）、documents（拖拽行 draggable+行点击+操作列）、presentations（缩略图+收藏+操作列）、datasets 动态列+列画像（2 个）、home 最近列表、billing 用量明细、publishes 发布管理、asset-detail 信息+版本（2 个）、settings（Git/成员/Token/发布/审计 5 个表格）。
- 关键发现：antd Table 组件 token 仅含 headerBg/headerColor/rowHoverBg/cellPadding*/borderColor/cellFontSize/headerBorderRadius/selectionColumnWidth，不含 tableXxx 前缀字段（如 tableHeaderBg 等会 typecheck 报错）。
- 清理旧样式：assets.ts 删 sg-assets-table 表头/行/列宽样式、document-tree.ts 删 sg-docs-table 相关（含 media query）、presentations.ts 删 sg-presentation-table（含 media query）、home.ts 删 sg-recent-table、shell.ts 删 sg-table，共 -230 行。
- 移除 @shiguang/ui 的 Table import（所有页面不再使用），datasets 同时移除 Scrollbar（antd Table scroll prop 替代）。
- 验证：tsc -b 全通过；`sg-table` 类名在 tsx 中零残留。
- 注意：@shiguang/ui 的 Table 组件本身保留（其他地方可能引用），但 web 已无引用。

## 修复 presentations 页面字体过小
- 原样式有 30+ 处 font-size 在 4~11px 之间（4px 缩略图字幕、6px 图表底标、7px 标签、8px 正文等），为密集仪表盘设计时故意压小，但实际可读性差。
- 统一调整为规整值（无小数/奇数值）：11px（装饰缩略图字幕）、12px（提示/标签/图表）、13px（正文/搜索/分页）、14px（副标题/侧栏标题）、20px/24px（标题保持）。
- 共调整 28 处 font-size 声明，tsc 通过。

## 修复 AppTable 的 whiteSpace: nowrap 导致缩略图被裁剪
- 根因：AppTable createStyles 中 `.ant-table-tbody > tr > td { whiteSpace: nowrap }` 导致 antd Table 按单行文本计算行高，presentations 表格中的 82×46px 缩略图封面被裁剪。
- 修复：移除 td 的 whiteSpace: nowrap（表头 th 保留），文本截断由各列 `ellipsis` 属性自行控制。
- presentations 表格第一列补 `width: "47%"`（对齐旧布局），确保缩略图有足够空间。

## 修复 presentations 缩略图尺寸与文字不匹配
- 字体从 4~9.5px 调大到 11~14px 后，82×46px 的缩略图盒子装不下 12px 文字，导致溢出/显示异常。
- 缩略图从 82px 扩大到 110px（16:9 → 62px 高），内部 padding 7→8px，伪元素装饰等比例缩放（~1.34×）。
- 缩略图文字微调：b 12px→12px（保持）、small 11px→11px（保持），网格视图 b 13px→12px。

## 统一 OwnerAvatar 组件（所有者/创建者显示）
- 新建 `apps/web/src/shared/OwnerAvatar.tsx`：圆形首字符头像（sg-owner-avatar，28px）+ Tooltip 悬浮显示全名 + role="img"/aria-label。
- presentations 创建者列从 @shiguang/ui Avatar+文字改为 OwnerAvatar；documents 移除本地 OwnerAvatar 改用共享组件；assets 移除内联 Tooltip 头像改用共享组件。
- 移除各页不再使用的 antd Tooltip import。

## 修复设置页首页菜单误高亮
- 根因：shell/layout.tsx 的 activeKey 计算中，找不到匹配导航项时回退返回 `"/"`（首页），导致 /settings、/billing 等独立路由页面首页菜单被高亮。
- 修复：`if (pathname === "/") return "/"` 单独处理首页；其它路径不匹配任何 NAV 项时返回空字符串，不高亮任何主菜单。设置齿轮图标自身已有独立 active 逻辑不受影响。

## 表格可点击行补 pointer 光标
- 原因：documents/assets/home 三个表格用 onRow.onClick 实现行点击，但 antd Table 默认行光标是 default，无可点击提示。
- 修复：三个表格 onRow 加 `style: { cursor: "pointer" }`；billing/settings 等不可点击行表格不动。

## 清理 git 误跟踪的生成物（.git 膨胀到 6.4G）
- 诊断：`.gitignore` 未忽略 `design/ChatGPT*`（98.8MB/75 张 PNG）、`.dsh-vision-router/artifacts/`（裁剪图）、`.playwright-cli/`（yml 快照）、`*.tsbuildinfo`（TS 编译缓存）。PNG 被反复重新生成提交导致 .git 累积到 6.4G。
- 修复：.gitignore 新增 4 条规则；`git rm --cached` 停止跟踪 87 个生成物（本地文件保留）。历史 blob 仍在 .git 中，如需真正瘦身需历史重写（filter-repo + gc，高风险未做）。


