# Shiguang Asset Hub

AI 原生知识与数字资产工作空间。仓库采用 pnpm/Turborepo 与 Go Workspace 组成的 monorepo。

## Requirements

- Node.js 22+
- pnpm 10+
- Go 1.24+

## Quick start

```bash
pnpm install
pnpm dev
```

完整校验：

```bash
pnpm check
```

`design/` 保存立项、需求与 UI 原始资料，应用代码位于 `apps/`，共享代码位于 `packages/`。

